export const createUser = async (
   firstName, lastName, email, password
  ) => {
    const requestBody = {
      firstName: firstName,
      lastName: lastName,
      email: email,
      password, password
    };
  };