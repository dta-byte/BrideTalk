import Parse from 'parse'
// Function skal hente thread, og få fat i beskeder derfra. 
export const getLiveChat = (username) => { 
    const threds =  getUserThreads(); 
    try {
        
    } catch (error) {
        
    }

 }